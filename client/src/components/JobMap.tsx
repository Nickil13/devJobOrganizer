import { Paper, Typography } from "@mui/material";
import React from "react";

const JobMap: React.FC<{}> = () => {
    return (
        <section className="job-map">
            <Typography variant="h4">Job Map</Typography>
            <Paper variant="outlined"></Paper>
        </section>
    );
};

export default JobMap;
